package begin_a_gain.omokwang.user.repository;

import begin_a_gain.omokwang.user.dto.DeletionSurvey;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeletionSurveyRepository extends JpaRepository<DeletionSurvey, Long> {
}
